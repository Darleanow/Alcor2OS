/**
 * @file shell/shell.h
 * @brief Shell-internal constants. The cross-component sh_* surface lives in
 *        <vega/host.h>, which this header pulls in for convenience.
 */

#ifndef SHELL_H
#define SHELL_H

#include <vega/host.h>

#define MAX_CMD_LEN 256

#endif /* SHELL_H */
